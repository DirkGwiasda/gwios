@page "/"
@inject NavigationManager NavigationManager

@code {
    // The start page is the ToDo overview until the portal gets a page of its own.
    protected override void OnInitialized()
        => NavigationManager.NavigateTo("todos", replace: true);
}
